services:
  node_exporter:
    image: quay.io/prometheus/node-exporter:v1.7.0
    container_name: node_exporter
    restart: always
    volumes:
      - '/:/host:ro,rslave'
    networks:
      - s-network

networks:
  s-network:
    external: true
