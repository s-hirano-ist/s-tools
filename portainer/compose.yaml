services:

  portainer:
    container_name: portainer
    image: portainer/portainer-ce:2.29.2-alpine@sha256:774952fc6704f07f5439383ef61fbdb37641ea163965856cde914036e026576a
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '10'
    restart: always
    volumes:
      - portainer_data:/data
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - s-network

volumes:
  portainer_data:

networks:
  s-network:
    external: true
